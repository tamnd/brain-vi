---
title: "CF 102697087 - Robot"
description: "Nhiệm vụ này được cố tình đơn giản. Đầu vào bao gồm một dòng văn bản và dòng này có thể chứa các ký tự ASCII tùy ý. Đầu ra được yêu cầu là dòng chính xác được in ba lần, với mỗi bản sao trên một dòng riêng."
date: "2026-08-15T09:40:48+07:00"
tags: ["codeforces", "competitive-programming"]
categories: ["algorithms"]
codeforces_contest: 102697
codeforces_index: "087"
codeforces_contest_name: "CodeRams Practice Problem Archive"
rating: 0
weight: 102697
solve_time_s: 40
verified: true
draft: false
---

[CF 102697087 - Người máy](https://codeforces.com/problemset/problem/102697/087) 

**Đánh giá:** - 
**Thẻ:** - 
**Thời gian giải quyết:** 40s 
**Đã xác minh:** có 

## Giải pháp 
## Hiểu vấn đề 

Nhiệm vụ này được cố tình đơn giản. Đầu vào bao gồm một dòng văn bản và dòng này có thể chứa các ký tự ASCII tùy ý. Đầu ra được yêu cầu là dòng chính xác được in ba lần, với mỗi bản sao trên một dòng riêng. 

Không có tính toán số, phân tích từ hoặc chuyển đổi văn bản. Mối quan tâm triển khai chính là bảo toàn đầu vào một cách chính xác. Đặc biệt, việc sử dụng trình đọc dựa trên mã thông báo sẽ sai vì dấu cách là một phần của dòng và phải duy trì ở đầu ra. Chúng ta cũng chỉ nên xóa dấu kết thúc dòng thay vì loại bỏ các khoảng trắng ở đầu hoặc cuối. 

Kích thước đầu vào không yêu cầu bất kỳ thuật toán phức tạp nào. Ngay cả khi dòng lớn, chúng ta chỉ cần đọc một lần và viết ba bản, do đó thời gian chạy tỷ lệ thuận với số ký tự đầu vào. Cách tiếp cận bậc hai sẽ không cần thiết, nhưng ở đây không có khó khăn về mặt thuật toán có ý nghĩa. 

Trường hợp cạnh hữu ích là một dòng chứa khoảng trắng, chẳng hạn như`A B`. Đầu ra đúng là:```
A B
A B
A B
```Việc thực hiện bất cẩn bằng cách sử dụng`split()`và việc nối các từ thu được có thể vô tình làm thay đổi khoảng cách. Một trường hợp cạnh khác là một đường có dấu cách ở cuối, chẳng hạn như`abc   `. Những khoảng trống đó thuộc về đầu vào và cần được sao chép. Đang gọi`strip()`sẽ âm thầm loại bỏ chúng và đưa ra câu trả lời sai. 

Đầu vào có ý nghĩa ngắn nhất có thể là một ký tự đơn:```
X
```Đầu ra phải là:```
X
X
X
```Quy tắc tương tự được áp dụng bất kể ký tự ASCII nào xuất hiện trong dòng. 

## Phương pháp tiếp cận 

Cách tiếp cận vũ phu về cơ bản đã là cách tiếp cận tối ưu vì không có gì để tìm kiếm hay tính toán. Chúng tôi đọc dòng hoàn chỉnh, sau đó thực hiện hai lần ghi bổ sung sau bản gốc, tạo ra ba dòng giống hệt nhau. Nếu dòng đầu vào chứa`n`ký tự, tiến trình của chương trình`3n`ký tự đầu ra, do đó công việc là O(n). 

Một cách tiếp cận phức tạp hơn có thể chia dòng thành các từ, xây dựng lại nó và sau đó in kết quả ba lần. Điều đó vẫn mất O(n) thời gian, nhưng nó tạo ra những cơ hội không cần thiết để thay đổi khoảng trắng. Cấu trúc của bài toán cho chúng ta biết rằng toàn bộ dòng là dữ liệu, do đó sự trừu tượng hóa chính xác chỉ đơn giản là một chuỗi chuỗi hoặc byte. 

Việc triển khai an toàn nhất sẽ đọc dòng dưới dạng byte. Điều này tránh mọi lo ngại về mã hóa ký tự không cần thiết vì đầu vào rõ ràng là ASCII. Chúng tôi xóa chính xác một ký tự dòng mới ở cuối và sau đó ghi byte kết quả ba lần. 

| Tiếp cận | Độ phức tạp thời gian | Độ phức tạp của không gian | Phán quyết | 
| --- | --- | --- | --- | 
| Token hóa và xây dựng lại | O(n) | O(n) | Được chấp nhận, nhưng không cần thiết | 
| Đọc toàn bộ dòng và in nó ba lần | O(n) | O(n) | Đã chấp nhận | 

## Hướng dẫn thuật toán 

1. Đọc toàn bộ dòng đầu vào dưới dạng chuỗi byte. Toàn bộ dòng phải được giữ nguyên vì dấu cách và các ký tự ASCII khác là một phần của đầu ra được yêu cầu. 
2. Xóa ký tự dòng mới cuối cùng khỏi đầu vào. Chúng tôi xóa dấu phân cách được thêm vào bởi định dạng đầu vào nhưng không loại bỏ bất kỳ khoảng trắng nào khác. 
3. Viết dòng kết quả theo sau là dòng mới ba lần. Mỗi lần viết đại diện cho một bản sao bắt buộc của văn bản đầu vào. 

### Tại sao nó hoạt động 

Đặt dòng đầu vào sau khi loại bỏ dòng mới kết thúc của nó là`s`. Đầu ra của thuật toán`s`, rồi cái khác`s`, rồi cái khác`s`, theo sau mỗi dòng là một dòng mới. Do đó, mọi ký tự của dòng gốc xuất hiện theo cùng thứ tự và khoảng cách như nhau ở cả ba dòng đầu ra. Không có chuyển đổi nào được thực hiện, vì vậy đầu ra chính xác là ba bản sao được yêu cầu. 

## Giải pháp Python```python
import sys
input = sys.stdin.readline

def solve():
    data = sys.stdin.buffer.readline()

    if data.endswith(b'\n'):
        data = data[:-1]

    sys.stdout.buffer.write(data + b'\n')
    sys.stdout.buffer.write(data + b'\n')
    sys.stdout.buffer.write(data + b'\n')

if __name__ == "__main__":
    solve()
```Chương trình sử dụng`sys.stdin.buffer.readline()`vì vậy đầu vào được xử lý trực tiếp dưới dạng byte. Vì vấn đề đảm bảo đầu vào ASCII nên điều này là đủ và tránh đưa ra các chuyển đổi mã hóa. 

biểu thức`data[:-1]`chỉ được sử dụng khi dòng thực sự kết thúc bằng`\n`. Điều này tốt hơn để`strip()`, bởi vì`strip()`sẽ xóa khoảng trắng và khoảng trắng khác ở đầu hoặc cuối dòng. Những ký tự đó có thể là đầu vào có ý nghĩa. 

Ba lần viết tương ứng trực tiếp với ba bản sao được yêu cầu. Không cần vòng lặp, mảng hoặc cấu trúc dữ liệu khác. Các số nguyên có độ chính xác tùy ý của Python không liên quan vì bài toán không chứa số học. 

## Ví dụ đã hoạt động 

Đối với mẫu được cung cấp, dòng đầu vào là`We Are The Robots`. 

| Bước | Dòng được lưu trữ trong`data`| Đầu ra | 
| --- | --- | --- | 
| 1 |`We Are The Robots`|`We Are The Robots`| 
| 2 |`We Are The Robots`|`We Are The Robots`| 
| 3 |`We Are The Robots`|`We Are The Robots`| 

Kết quả đầu ra là:```
We Are The Robots
We Are The Robots
We Are The Robots
```Dấu vết này cho thấy các khoảng trống bên trong dòng ban đầu được giữ nguyên. Thuật toán không diễn giải văn bản thành các từ riêng biệt. 

Đối với một dòng chứa dấu cách bên trong và dấu cách, hãy xem xét:```
A  B
```Sau khi chỉ xóa dòng mới, dữ liệu được lưu trữ vẫn còn`A  B   `. 

| Bước | Dòng được lưu trữ | Hành động | 
| --- | --- | --- | 
| 1 |`A  B   `| Viết dòng và dòng mới | 
| 2 |`A  B   `| Viết dòng và dòng mới | 
| 3 |`A  B   `| Viết dòng và dòng mới | 

Đầu ra chứa chính xác hai khoảng trắng giống nhau giữa`A`Và`B`và ba khoảng trắng tương tự sau`B`. Điều này chứng tỏ tại sao`strip()`hoặc mã thông báo sẽ không an toàn. 

## Phân tích độ phức tạp 

| Đo | Độ phức tạp | Giải thích | 
| --- | --- | --- | 
| Thời gian | O(n) | Đầu vào chứa`n`ký tự và chương trình ghi ba bản sao, đó là 3n phép toán tiệm cận O(n). | 
| Không gian | O(n) | Dòng đầu vào được lưu trữ một lần trước khi được viết. | 

Thuật toán dễ dàng nằm trong giới hạn yêu cầu vì nó chỉ thực hiện một số lần chuyển không đổi qua đầu vào. Không có sự phụ thuộc vào số lượng từ hoặc bất kỳ trạng thái tổ hợp ẩn nào. 

## Trường hợp thử nghiệm```python
# helper: run solution on input string, return output string
import sys
import io

def solve():
    data = sys.stdin.buffer.readline()

    if data.endswith(b'\n'):
        data = data[:-1]

    sys.stdout.buffer.write(data + b'\n')
    sys.stdout.buffer.write(data + b'\n')
    sys.stdout.buffer.write(data + b'\n')

def run(inp: str) -> str:
    old_stdin = sys.stdin
    old_stdout = sys.stdout

    sys.stdin = io.TextIOWrapper(io.BytesIO(inp.encode('ascii')))
    output = io.BytesIO()
    sys.stdout = io.TextIOWrapper(output)

    try:
        solve()
        sys.stdout.flush()
        return output.getvalue().decode('ascii')
    finally:
        sys.stdin = old_stdin
        sys.stdout = old_stdout

# provided sample
assert run("We Are The Robots\n") == (
    "We Are The Robots\n"
    "We Are The Robots\n"
    "We Are The Robots\n"
), "sample"

# minimum-size case
assert run("X\n") == "X\nX\nX\n", "single character"

# all spaces
assert run("   \n") == "   \n   \n   \n", "spaces must be preserved"

# internal and trailing spaces
assert run("A  B   \n") == (
    "A  B   \n"
    "A  B   \n"
    "A  B   \n"
), "whitespace preservation"

# a larger ASCII line
s = "ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789 !@#$%^&*()"
assert run(s + "\n") == s + "\n" + s + "\n" + s + "\n", "ASCII preservation"
```| Kiểm tra đầu vào | Sản lượng dự kiến ​​| Nó xác nhận những gì | 
| --- | --- | --- | 
|`X`|`X`ba lần | Đầu vào kích thước tối thiểu | 
| Ba không gian | Ba dấu cách ba lần | Khoảng trắng đầu và cuối | 
|`A  B   `| Cùng một dòng ba lần | Nhiều dấu cách và dấu cách | 
| Chữ cái, chữ số và ký hiệu ASCII | Cùng một dòng ba lần | Bảo quản ASCII chung | 

## Vỏ cạnh 

Đối với đầu vào một ký tự, chẳng hạn như`X`, thuật toán đọc`X\n`, chỉ loại bỏ`\n`, và viết`X\n`ba lần. Đầu ra chính xác là ba bản sao của văn bản gốc. 

Đối với một đầu vào bao gồm toàn bộ khoảng trắng, chẳng hạn như`   `, thuật toán sẽ đọc các khoảng trắng theo sau là dòng mới. Chỉ xóa dòng mới để giữ nguyên cả ba khoảng trắng, do đó đầu ra là ba dòng chứa ba khoảng trắng. Một triển khai sử dụng`strip()`sẽ biến đầu vào thành một chuỗi trống không chính xác. 

Đối với đầu vào có khoảng trắng bên trong lặp đi lặp lại, chẳng hạn như`A  B`, hai khoảng trắng giữa các ký tự vẫn được giữ nguyên. Thuật toán xử lý toàn bộ dòng dưới dạng một chuỗi byte thay vì chia nó thành các mã thông báo, do đó khoảng cách không thể bị chuẩn hóa một cách vô tình. 

Đối với đầu vào có dấu cách ở cuối, chẳng hạn như`abc   `, chỉ dòng mới kết thúc bị xóa. Ba khoảng trống theo sau`c`vẫn còn trong dòng được lưu trữ và do đó xuất hiện ở cuối mỗi bản sao đầu ra. 

Việc triển khai cũng xử lý dấu câu ASCII thông thường mà không có trường hợp đặc biệt. Những nhân vật như`!`,`@`,`#`,`$`, Và`%`chỉ đơn giản là các byte trong chuỗi đầu vào nên chúng được sao chép không thay đổi.
